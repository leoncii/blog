import { BlogPost } from '../../../containers/BlogPost'
import Highlight, { defaultProps } from "prism-react-renderer"

export const meta = {
  title: 'Eslint en 1 minuto',
  description: 'Configura tu eslint en un minuto',
  date: 'Jul 14, 2021',
  readTime: 2,
  imgUrl: '',
  isActive: true
}

export default function Eslint({children}) {
    return <BlogPost meta={meta}>{children}</BlogPost>
}


npm install --save-dev babel-eslint eslint-config-standard eslint-config-standard-jsx eslint-config-standard-react eslint-plugin-promise eslint-plugin-import eslint-plugin-node eslint-plugin-react


"eslintConfig": {
  "parser": "babel-eslint",
  "extends": ["standard", "standard-jsx", "standard-react"]
}